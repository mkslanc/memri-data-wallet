.filter-starred {
    inherit: {{view}}

    title: "Starred {view.title}"

    [datasource = pod] {
        query: "Person"
        filter: {
            properties: {
               starred: true
            }
        }
    }
}
